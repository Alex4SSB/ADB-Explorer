<!--
    This Source Code Form is subject to the terms of the MIT License.
    If a copy of the MIT was not distributed with this file, You can obtain one at https://opensource.org/licenses/MIT.
    Copyright (C) Leszek Pomianowski and WPF UI Contributors.
    All Rights Reserved.
    
    Based on Microsoft XAML for Win UI
    Copyright (c) Microsoft Corporation. All Rights Reserved.
-->

<!--  Copied from WPF UI for modification  -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:ADB_Explorer.Converters"
    xmlns:helpers="clr-namespace:ADB_Explorer.Helpers"
    xmlns:models="clr-namespace:ADB_Explorer.Models"
    xmlns:sys="clr-namespace:System;assembly=System.Runtime"
    xmlns:system="clr-namespace:System;assembly=System.Runtime"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Thickness x:Key="ExpanderPadding">11,11,11,11</Thickness>
    <Thickness x:Key="ExpanderBorderThemeThickness">1</Thickness>
    <system:Double x:Key="ExpanderChevronSize">16.0</system:Double>
    <Duration x:Key="AnimationDuration">00:00:00.3</Duration>
    <sys:TimeSpan x:Key="AnimationKeytime">00:00:00.2</sys:TimeSpan>

    <converters:AnimationFactorToValueConverter x:Key="AnimationFactorToValueConverter" />

    <Storyboard x:Key="RotateTo180CwStoryboard">
        <DoubleAnimation
            Storyboard.TargetName="ArrowRotateTransform"
            Storyboard.TargetProperty="Angle"
            From="1"
            To="179"
            Duration="{StaticResource AnimationDuration}" />
    </Storyboard>

    <Storyboard x:Key="RotateTo0CwStoryboard">
        <DoubleAnimation
            Storyboard.TargetName="ArrowRotateTransform"
            Storyboard.TargetProperty="Angle"
            From="181"
            To="359"
            Duration="{StaticResource AnimationDuration}" />
    </Storyboard>

    <Storyboard x:Key="RotateTo180CcwStoryboard">
        <DoubleAnimation
            Storyboard.TargetName="ArrowRotateTransform"
            Storyboard.TargetProperty="Angle"
            From="359"
            To="181"
            Duration="{StaticResource AnimationDuration}" />
    </Storyboard>

    <Storyboard x:Key="RotateTo0CcwStoryboard">
        <DoubleAnimation
            Storyboard.TargetName="ArrowRotateTransform"
            Storyboard.TargetProperty="Angle"
            From="179"
            To="1"
            Duration="{StaticResource AnimationDuration}" />
    </Storyboard>

    <TranslateTransform x:Key="RollDownTransform">
        <TranslateTransform.Y>
            <MultiBinding Converter="{StaticResource AnimationFactorToValueConverter}" ConverterParameter="negative">
                <Binding ElementName="ContentPresenterBorder" Path="ActualHeight" />
                <Binding ElementName="ContentPresenterBorder" Path="(helpers:AnimationProperties.AnimationTagValue)" />
            </MultiBinding>
        </TranslateTransform.Y>
    </TranslateTransform>

    <TranslateTransform x:Key="RollUpTransform">
        <TranslateTransform.Y>
            <MultiBinding Converter="{StaticResource AnimationFactorToValueConverter}" ConverterParameter="positive">
                <Binding ElementName="ContentPresenterBorder" Path="ActualHeight" />
                <Binding ElementName="ContentPresenterBorder" Path="(helpers:AnimationProperties.AnimationTagValue)" />
            </MultiBinding>
        </TranslateTransform.Y>
    </TranslateTransform>

    <TranslateTransform x:Key="RollLeftTransform">
        <TranslateTransform.X>
            <MultiBinding Converter="{StaticResource AnimationFactorToValueConverter}" ConverterParameter="positive">
                <Binding ElementName="ContentPresenterBorder" Path="ActualWidth" />
                <Binding ElementName="ContentPresenterBorder" Path="(helpers:AnimationProperties.AnimationTagValue)" />
            </MultiBinding>
        </TranslateTransform.X>
    </TranslateTransform>

    <TranslateTransform x:Key="RollRightTransform">
        <TranslateTransform.X>
            <MultiBinding Converter="{StaticResource AnimationFactorToValueConverter}" ConverterParameter="negative">
                <Binding ElementName="ContentPresenterBorder" Path="ActualWidth" />
                <Binding ElementName="ContentPresenterBorder" Path="(helpers:AnimationProperties.AnimationTagValue)" />
            </MultiBinding>
        </TranslateTransform.X>
    </TranslateTransform>

    <ControlTemplate x:Key="DefaultExpanderToggleButtonStyle" TargetType="{x:Type ToggleButton}">
        <Grid Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid
                x:Name="ChevronGrid"
                Grid.Column="0"
                Margin="{TemplateBinding Padding}"
                VerticalAlignment="Center"
                Background="Transparent"
                RenderTransformOrigin="0.5, 0.5">
                <Grid.RenderTransform>
                    <RotateTransform x:Name="ArrowRotateTransform" />
                </Grid.RenderTransform>
                <ui:SymbolIcon
                    x:Name="ControlChevronIcon"
                    FontSize="{StaticResource ExpanderChevronSize}"
                    Foreground="{TemplateBinding Foreground}"
                    Symbol="ChevronDown24">
                    <ui:SymbolIcon.RenderTransform>
                        <RotateTransform Angle="0" />
                    </ui:SymbolIcon.RenderTransform>
                </ui:SymbolIcon>
            </Grid>
            <ContentPresenter
                x:Name="ContentPresenter"
                Grid.Column="1"
                Margin="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Content="{TemplateBinding Content}"
                TextElement.FontSize="{TemplateBinding FontSize}" />
        </Grid>
        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Expander}, Path=(helpers:ExpanderHelper.ChevronPlacement)}" Value="Right">
                <Setter TargetName="ChevronGrid" Property="Grid.Column" Value="2" />
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Expander}, Path=ExpandDirection}" Value="Left">
                <Setter TargetName="ControlChevronIcon" Property="RenderTransform">
                    <Setter.Value>
                        <RotateTransform Angle="90" />
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Expander}, Path=ExpandDirection}" Value="Up">
                <Setter TargetName="ControlChevronIcon" Property="RenderTransform">
                    <Setter.Value>
                        <RotateTransform Angle="180" />
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Expander}, Path=ExpandDirection}" Value="Right">
                <Setter TargetName="ControlChevronIcon" Property="RenderTransform">
                    <Setter.Value>
                        <RotateTransform Angle="270" />
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Mode=Self}, Path=IsChecked}" Value="True" />
                    <Condition Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Expander}, Path=(helpers:ExpanderHelper.ExpanderArrow)}" Value="CCW" />
                </MultiDataTrigger.Conditions>
                <MultiDataTrigger.EnterActions>
                    <BeginStoryboard Storyboard="{StaticResource RotateTo180CcwStoryboard}" />
                </MultiDataTrigger.EnterActions>
                <MultiDataTrigger.ExitActions>
                    <BeginStoryboard Storyboard="{StaticResource RotateTo0CcwStoryboard}" />
                </MultiDataTrigger.ExitActions>
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Mode=Self}, Path=IsChecked}" Value="True" />
                    <Condition Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Expander}, Path=(helpers:ExpanderHelper.ExpanderArrow)}" Value="CW" />
                </MultiDataTrigger.Conditions>
                <MultiDataTrigger.EnterActions>
                    <BeginStoryboard Storyboard="{StaticResource RotateTo180CwStoryboard}" />
                </MultiDataTrigger.EnterActions>
                <MultiDataTrigger.ExitActions>
                    <BeginStoryboard Storyboard="{StaticResource RotateTo0CwStoryboard}" />
                </MultiDataTrigger.ExitActions>
            </MultiDataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Expander}, Path=(helpers:ExpanderHelper.ExpanderArrow)}" Value="None">
                <Setter TargetName="ChevronGrid" Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="AdbExpanderStyle" TargetType="{x:Type Expander}">
        <!--  Universal WPF UI focus  -->
        <Setter Property="FocusVisualStyle" Value="{DynamicResource DefaultControlFocusVisualStyle}" />
        <!--  Universal WPF UI focus  -->
        <Setter Property="Background" Value="{DynamicResource ExpanderHeaderBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource ExpanderHeaderForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ExpanderHeaderBorderBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource ExpanderBorderThemeThickness}" />
        <Setter Property="Padding" Value="{StaticResource ExpanderPadding}" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Border.CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="IsExpanded" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="helpers:ExpanderHelper.ExpanderArrow" Value="CCW" />
        <Setter Property="helpers:ExpanderHelper.ChevronPlacement" Value="Right" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Expander}">
                    <DockPanel x:Name="DockPanel">

                        <!--  Top level controls always visible  -->
                        <Border
                            x:Name="ToggleButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="{TemplateBinding Border.CornerRadius}">
                            <ToggleButton
                                x:Name="ExpanderToggleButton"
                                Padding="{TemplateBinding Padding}"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Center"
                                Content="{TemplateBinding Header}"
                                FontSize="{TemplateBinding FontSize}"
                                Foreground="{TemplateBinding Foreground}"
                                IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                IsEnabled="{TemplateBinding IsEnabled}"
                                OverridesDefaultStyle="True"
                                Template="{StaticResource DefaultExpanderToggleButtonStyle}"
                                ToolTip="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Expander}, Path=(helpers:ExpanderHelper.HeaderTooltip)}" />
                        </Border>

                        <!--  Collapsed content to expand  -->
                        <Grid x:Name="ContentGrid" ClipToBounds="True">
                            <Border
                                x:Name="ContentPresenterBorder"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1,0,1,1"
                                CornerRadius="0,0,4,4"
                                Visibility="Collapsed">
                                <ContentPresenter
                                    x:Name="ContentPresenter"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}" />
                            </Border>
                        </Grid>
                    </DockPanel>

                    <ControlTemplate.Triggers>
                        <Trigger Property="ExpandDirection" Value="Down">
                            <Setter TargetName="ContentGrid" Property="DockPanel.Dock" Value="Bottom" />
                            <Setter TargetName="ToggleButtonBorder" Property="DockPanel.Dock" Value="Top" />
                            <Setter TargetName="ContentPresenterBorder" Property="RenderTransform" Value="{StaticResource RollDownTransform}" />
                        </Trigger>
                        <Trigger Property="ExpandDirection" Value="Right">
                            <Setter TargetName="ContentGrid" Property="DockPanel.Dock" Value="Right" />
                            <Setter TargetName="ToggleButtonBorder" Property="DockPanel.Dock" Value="Left" />
                            <Setter TargetName="ContentPresenterBorder" Property="RenderTransform" Value="{StaticResource RollRightTransform}" />
                        </Trigger>
                        <Trigger Property="ExpandDirection" Value="Up">
                            <Setter TargetName="ContentGrid" Property="DockPanel.Dock" Value="Top" />
                            <Setter TargetName="ToggleButtonBorder" Property="DockPanel.Dock" Value="Bottom" />
                            <Setter TargetName="ContentPresenterBorder" Property="RenderTransform" Value="{StaticResource RollUpTransform}" />
                        </Trigger>
                        <Trigger Property="ExpandDirection" Value="Left">
                            <Setter TargetName="ContentGrid" Property="DockPanel.Dock" Value="Left" />
                            <Setter TargetName="ToggleButtonBorder" Property="DockPanel.Dock" Value="Right" />
                            <Setter TargetName="ContentPresenterBorder" Property="RenderTransform" Value="{StaticResource RollLeftTransform}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsExpanded" Value="True" />
                                <Condition Property="ExpandDirection" Value="Down" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ToggleButtonBorder" Property="CornerRadius" Value="4,4,0,0" />
                            <Setter TargetName="ContentPresenterBorder" Property="BorderThickness" Value="1,0,1,1" />
                            <Setter TargetName="ContentPresenterBorder" Property="CornerRadius" Value="0,0,4,4" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsExpanded" Value="True" />
                                <Condition Property="ExpandDirection" Value="Left" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ToggleButtonBorder" Property="CornerRadius" Value="0,4,4,0" />
                            <Setter TargetName="ContentPresenterBorder" Property="BorderThickness" Value="1,1,0,1" />
                            <Setter TargetName="ContentPresenterBorder" Property="CornerRadius" Value="4,0,0,4" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsExpanded" Value="True" />
                                <Condition Property="ExpandDirection" Value="Up" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ToggleButtonBorder" Property="CornerRadius" Value="0,0,4,4" />
                            <Setter TargetName="ContentPresenterBorder" Property="BorderThickness" Value="1,1,1,0" />
                            <Setter TargetName="ContentPresenterBorder" Property="CornerRadius" Value="4,4,0,0" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsExpanded" Value="True" />
                                <Condition Property="ExpandDirection" Value="Right" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ToggleButtonBorder" Property="CornerRadius" Value="4,0,0,4" />
                            <Setter TargetName="ContentPresenterBorder" Property="BorderThickness" Value="0,1,1,1" />
                            <Setter TargetName="ContentPresenterBorder" Property="CornerRadius" Value="0,4,4,0" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsExpanded" Value="True" />
                                <Condition Property="helpers:ExpanderHelper.IsTransparent" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ToggleButtonBorder" Property="CornerRadius" Value="4" />
                        </MultiTrigger>
                        <Trigger Property="IsExpanded" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenterBorder" Storyboard.TargetProperty="(Border.Visibility)">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static Visibility.Visible}" />
                                        </ObjectAnimationUsingKeyFrames>

                                        <DoubleAnimation
                                            BeginTime="00:00:00"
                                            Storyboard.TargetName="ContentPresenterBorder"
                                            Storyboard.TargetProperty="(helpers:AnimationProperties.AnimationTagValue)"
                                            To="0"
                                            Duration="{StaticResource AnimationDuration}">
                                            <DoubleAnimation.EasingFunction>
                                                <CircleEase EasingMode="EaseInOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenterBorder" Storyboard.TargetProperty="(Border.Visibility)">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static Visibility.Visible}" />
                                            <DiscreteObjectKeyFrame KeyTime="0:0:0.2" Value="{x:Static Visibility.Collapsed}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <DoubleAnimation
                                            BeginTime="00:00:00"
                                            Storyboard.TargetName="ContentPresenterBorder"
                                            Storyboard.TargetProperty="(helpers:AnimationProperties.AnimationTagValue)"
                                            To="1"
                                            Duration="{StaticResource AnimationDuration}">
                                            <DoubleAnimation.EasingFunction>
                                                <CircleEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="helpers:ExpanderHelper.IsTransparent" Value="False">
                            <Setter TargetName="ContentPresenterBorder" Property="Background" Value="{DynamicResource ExpanderContentBackground}" />
                        </Trigger>
                        <Trigger Property="helpers:ExpanderHelper.IsTransparent" Value="True">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Opacity="0" Color="{DynamicResource CardBackgroundFillColorDefault}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="BorderBrush" Value="Transparent" />
                            <Setter TargetName="ContentPresenterBorder" Property="Background" Value="Transparent" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource ExpanderHeaderDisabledForeground}" />
                            <Setter TargetName="ExpanderToggleButton" Property="Foreground" Value="{DynamicResource ExpanderHeaderDisabledForeground}" />
                            <Setter TargetName="ExpanderToggleButton" Property="BorderBrush" Value="{DynamicResource ExpanderHeaderDisabledBorderBrush}" />
                        </Trigger>
                        <Trigger SourceName="ExpanderToggleButton" Property="IsMouseOver" Value="True">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Opacity="{DynamicResource AccentButtonBackgroundMouseOverOpacity}" Color="{DynamicResource CardBackgroundFillColorDefault}" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger SourceName="ExpanderToggleButton" Property="IsPressed" Value="True">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Opacity="{DynamicResource AccentButtonBackgroundPressedOpacity}" Color="{DynamicResource CardBackgroundFillColorDefault}" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource AdbExpanderStyle}" TargetType="{x:Type Expander}" />

</ResourceDictionary>
