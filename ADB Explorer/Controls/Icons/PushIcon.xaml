<UserControl
    x:Class="ADB_Explorer.Controls.PushIcon"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ADB_Explorer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Name="Icon"
    Style="{StaticResource MenuHostedIconControlStyle}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style BasedOn="{StaticResource DisabledGlyphStyle}" TargetType="ui:FontIcon" />
    </UserControl.Resources>
    <Viewbox Stretch="Uniform">
        <controls:SimpleStackPanel
            Margin="-5,0"
            d:Spacing="-5"
            DataContext="{Binding ElementName=Icon}"
            FlowDirection="LeftToRight"
            Orientation="Horizontal"
            Spacing="{Binding Spacing}">
            <ui:FontIcon FontSize="18" Glyph="&#xE8EA;" />
            <ui:FontIcon
                d:Foreground="#4CC2FF"
                FontSize="14"
                Glyph="&#xE973;">
                <ui:FontIcon.Style>
                    <StaticResource ResourceKey="IconAccentGlyphStyle" />
                </ui:FontIcon.Style>
            </ui:FontIcon>
        </controls:SimpleStackPanel>
    </Viewbox>
</UserControl>
