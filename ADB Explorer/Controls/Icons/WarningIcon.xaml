<UserControl
    x:Class="ADB_Explorer.Controls.WarningIcon"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ADB_Explorer.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Name="WarningIconControl"
    mc:Ignorable="d">
    <Grid DataContext="{Binding ElementName=WarningIconControl}">
        <ui:FontIcon
            FontSize="{Binding FontSize}"
            FontWeight="Bold"
            Foreground="{StaticResource EditorTextChangedBrush}"
            Glyph="&#xF13A;" />
        <ui:FontIcon
            FontSize="{Binding FontSize}"
            FontWeight="Bold"
            Foreground="Black"
            Glyph="&#xF13B;" />
    </Grid>
</UserControl>
