namespace ADB_Explorer.Models
{
    public enum UserPrefs
    {
        defaultFolder,
        pullOnDoubleClick,
        rememberIp,
        lastIp,
        rememberPort,
        lastPort,
        autoOpen,
        showExtensions,
        showHiddenItems,
        showExtendedView,
        enableMdns,
        manualAdbPath,
        forceFluentStyles,
    }

    public enum SystemVals
    {
        windowMaximized,
        detailedVisible,
        detailedHeight,
    }

    public enum AppTheme
    {
        windowsDefault,
        light,
        dark,
    }
}
